# Iteration 2 Checkpoint Packet

## Goal
Run a controlled review with Nika and Lilia on chair outputs and the new tabbed TZ form.

## What to demo
1. New TZ modal structure (intro + slide tabs).
2. One full chair render run in Figma plugin.
3. Saved outputs with normalized naming:
   - `<variant_id_or_article>_<index>_<slide_type>.png`
4. Checkpoint comments storage from admin dashboard:
   - Button: `+ Чекпойнт комментарий`.

## Review checklist
- Use: `docs/checkpoint_review_template.md`
- Focus on blocking issues only:
  - wrong photo type per slide,
  - wrong product/variant image,
  - bad arrow targeting / dimensions mismatch,
  - missing required slide,
  - incorrect sequence.

## After-call protocol
1. Save all comments in job checkpoint notes.
2. Mark each issue as:
   - `content_rule`,
   - `selection_logic`,
   - `template_mapping`,
   - `ui_tz`.
3. Start next patch cycle only after issue list is grouped by type.

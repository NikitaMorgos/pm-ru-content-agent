[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn content_agent.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

[environments.production.variables]
APP_ENV = "production"
LOG_LEVEL = "INFO"
